/** Sidebar width in px when expanded (full list visible). */
export const SIDEBAR_WIDTH_EXPANDED = '320px';

/** Sidebar width in px when collapsed (icon strip only). */
export const SIDEBAR_WIDTH_COLLAPSED = '56px';
